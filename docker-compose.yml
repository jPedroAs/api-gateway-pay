version: '3.7'

services:
  api:
    image: api-dotnet-render
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      - DefaultConnection=${DefaultConnection}
      - Secret=${Secret}